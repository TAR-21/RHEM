## 🛠️ 自動生成クライアントの利用手順

自動生成クライアントを使う際の流れは以下の 3 ステップです。

```
[1. インポート] ──► [2. 初期化 (Configuration)] ──► [3. API 関数の実行]

```

---

### Step 1: クライアントの作成（生成処理）

まず、CLI を使ってフロントエンドのプロジェクト内にクライアントコードを出力します。

```bash
# プロジェクトの src/api/generated などに出力
npx @openapitools/openapi-generator-cli generate \
  -i openapi.yaml \
  -g typescript-fetch \
  -o ./src/api/generated

```

これで `./src/api/generated` 配下に、各リソースに対応した API クラス（例: `DevicesApi`, `FleetsApi`）や型定義が自動作成されます。

---

### Step 2: クライアントの初期化（共通インスタンスの作成）

API サーバの URL や認証トークン（JWT）、ヘッダー情報を設定した共通のクライアントインスタンスを作成します。

```typescript
// src/api/flightctlClient.ts
import { Configuration, DevicesApi, FleetsApi } from './generated';

// 1. 設定オブジェクトの作成
const config = new Configuration({
  // ベースURLを設定
  basePath: 'https://your-rhem-api-server/api/v1',
  
  // Bearer トークンの設定 (関数形式で動的に最新トークンを返すことも可能)
  accessToken: () => {
    return localStorage.getItem('jwt_token') || '';
  },
  
  // カスタムヘッダー（APIバージョンの指定など）
  headers: {
    'Flightctl-API-Version': 'v1beta1',
  },
});

// 2. 必要な API クラスのインスタンス化
export const devicesApi = new DevicesApi(config);
export const fleetsApi = new FleetsApi(config);

```

---

### Step 3: React などのコンポーネントからの呼び出し

あとは、作成した API インスタンスを呼び出すだけです。引数や戻り値に型が自動適用されるため、タイポや型エラーをコンパイル時に検知できます。

#### 🔹 データ取得（GET）の例

```typescript
// src/components/DeviceList.tsx
import React, { useEffect, useState } from 'react';
import { devicesApi } from '../api/flightctlClient';
import { Device } from '../api/generated'; // 自動生成された型定義

export const DeviceList = () => {
  const [devices, setDevices] = useState<Device[]>([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    async function fetchDevices() {
      try {
        // メソッド名や引数が型補完されます
        // labelSelector などのクエリパラメータもオブジェクトで渡せる
        const response = await devicesApi.listDevices({
          labelSelector: 'site=factory-a',
        });
        
        // TypeScript が response.items を Device[] だと認識します
        setDevices(response.items || []);
      } catch (error) {
        console.error('Failed to fetch devices:', error);
      } finally {
        setLoading(false);
      }
    }

    fetchDevices();
  }, []);

  if (loading) return <div>Loading...</div>;

  return (

      <h2>Devices</h2>
      <ul>
        {devices.map((device) => (
          <li key={device.metadata?.name}>
            {device.metadata?.name} - Status: {device.status?.summary?.status}
          </li>
        ))}
      </ul>
    
  );
};

```

#### 🔹 データ更新（PUT / POST）の例

```typescript
import { devicesApi } from '../api/flightctlClient';

// デバイスのイメージを更新する関数
async function updateDeviceImage(deviceName: string, newImage: string) {
  try {
    // 1. 現在のオブジェクトを取得 (型安全)
    const device = await devicesApi.getDevice({ name: deviceName });

    // 2. 更新用データを作成
    if (device.spec) {
      device.spec.os = { image: newImage };
    }

    // 3. リクエスト送信 (resourceVersion による楽観的排他制御も自動処理)
    const updatedDevice = await devicesApi.replaceDevice({
      name: deviceName,
      device: device, // Payloadの型が不一致だとビルドエラーになります
    });

    console.log('Successfully updated:', updatedDevice);
  } catch (error) {
    console.error('Update failed:', error);
  }
}

```

---

## 💡 自動生成クライアントを使うメリット

| 手動 `fetch` の場合 | 自動生成クライアントの場合 |
| --- | --- |
| エンドポイントの URL 文字列を手動入力（タイポの危険） | 関数呼び出しのみ（例: `devicesApi.listDevices()`） |
| レスポンスの型を `any` または手動インターフェース定義 | OpenAPI の定義から**完全な TypeScript 型が自動設定** |
| クエリ文字列の組み立て（`URLSearchParams` 等）が必要 | 引数オブジェクトを渡すだけで自動シリアライズ |
| API の変更時にフロントコードが静かに壊れる | API 仕様変更時に生成コードを更新すれば**ビルドエラーで即検知** |

---

## 🔄 業務でのおすすめ運用パターン

実務では、`React Query` (`@tanstack/react-query`) や `SWR` と組み合わせるのが一般的です。

```typescript
import { useQuery } from '@tanstack/react-query';
import { devicesApi } from '../api/flightctlClient';

export function useDevices(site: string) {
  return useQuery({
    queryKey: ['devices', site],
    queryFn: () => devicesApi.listDevices({ labelSelector: `site=${site}` }),
  });
}

```

このようにラップして使うと、**「自動生成による強力な型安全」** と **「React Query によるキャッシュ・自動再取得」** の両方の恩恵を受けられます。
